// Production environment configuration
export const config = {
  apiUrl: 'https://api.sportsbookingapp.com/api',
  environment: 'production',
  enableLogging: false,
  enableReduxDevTools: false,
  cacheTimeout: 30 * 60 * 1000, // 30 minutes
};