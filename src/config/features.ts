/**
 * Feature flags for the app.
 * Toggle features on/off without removing code.
 */

/** When false, all subscription/membership upsell UI is hidden and tier checks are bypassed. */
export const MEMBERSHIPS_ENABLED = false;
